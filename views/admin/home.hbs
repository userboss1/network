<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - JDT ToolHub</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #4361ee;
            --primary-dark: #3a56d4;
            --primary-light: #eef2ff;
            --accent: #f72585;
            --success: #10b981;
            --danger: #ef4444;
            --info: #06b6d4;
            --warning: #f59e0b;
            --text: #1f2937;
            --text-light: #6b7280;
            --background: #f9fafb;
            --card-bg: #ffffff;
            --border: #e5e7eb;
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-md: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --transition: all 0.2s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--background);
            color: var(--text);
            min-height: 100vh;
            display: grid;
            grid-template-columns: 1fr;
            grid-template-rows: auto 1fr;
        }

      header {
  background: var(--card-bg);
  padding: 1rem 2rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid var(--border);
  box-shadow: var(--shadow-sm);
  position: sticky;
  top: 0;
  z-index: 10;
}


        .logo-container {
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .logo-text {
            font-weight: 700;
            font-size: 1.25rem;
            background: linear-gradient(90deg, var(--primary) 0%, var(--accent) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .admin-profile {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 0.5rem 1rem;
            background: var(--primary-light);
            border-radius: 2rem;
            cursor: pointer;
            transition: var(--transition);
        }

        .admin-profile:hover {
            box-shadow: var(--shadow);
        }

        .profile-icon {
            width: 2rem;
            height: 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--primary);
            color: white;
            border-radius: 50%;
            font-weight: 600;
        }

        .admin-name {
            font-weight: 500;
            color: var(--primary-dark);
        }

        .main-content {
            padding: 2rem;
            width: 100%;
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text);
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .actions-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .btn {
            padding: 0.75rem 1.25rem;
            border-radius: 0.5rem;
            font-size: 0.875rem;
            font-weight: 600;
            text-decoration: none;
            color: white;
            background: var(--primary);
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            border: none;
            cursor: pointer;
            box-shadow: var(--shadow-sm);
        }

        .btn:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .btn-icon {
            font-size: 1.25rem;
        }

        .viva-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(400px, 1fr));
            gap: 1.5rem;
        }

        .viva-card {
            background: var(--card-bg);
            border-radius: 0.75rem;
            overflow: hidden;
            transition: var(--transition);
            border: 1px solid var(--border);
            box-shadow: var(--shadow-sm);
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .viva-card:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-md);
        }

        .viva-header {
            padding: 1.25rem;
            font-size: 1.125rem;
            font-weight: 600;
            border-bottom: 1px solid var(--border);
            color: var(--primary-dark);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .viva-badge {
            font-size: 0.75rem;
            padding: 0.25rem 0.75rem;
            border-radius: 1rem;
            background: var(--primary-light);
            color: var(--primary-dark);
        }

        .viva-content {
            padding: 1.25rem;
            display: flex;
            flex-direction: column;
            gap: 1.25rem;
            flex: 1;
        }

        .viva-info {
            display: grid;
            grid-template-columns: auto 1fr;
            gap: 0.75rem 1rem;
            padding: 1rem;
            background: var(--primary-light);
            border-radius: 0.5rem;
        }

        .viva-info-label {
            color: var(--text-light);
            font-weight: 500;
            font-size: 0.875rem;
        }

        .viva-info-value {
            font-weight: 500;
            color: var(--text);
        }

        .actions {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 0.75rem;
        }

        .action-btn {
            padding: 0.75rem 1rem;
            font-size: 0.875rem;
            border-radius: 0.5rem;
            text-decoration: none;
            font-weight: 600;
            text-align: center;
            border: none;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
            box-shadow: var(--shadow-sm);
        }

        .assign {
            background: var(--primary);
            color: white;
        }

        .assign:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .view-assigned {
            background: var(--info);
            color: white;
        }

        .view-assigned:hover {
            background: #0891b2;
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .results {
            background: var(--success);
            color: white;
        }

        .results:hover {
            background: #059669;
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .delete {
            background: var(--danger);
            color: white;
        }

        .delete:hover {
            background: #dc2626;
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .remove {
            background: var(--danger);
            color: white;
        }

        .remove:hover {
            background: #dc2626;
        }

        .collapsible-section {
            margin-top: 1rem;
            border-top: 1px solid var(--border);
            padding-top: 1rem;
        }

        .collapsible {
            cursor: pointer;
            width: 100%;
            text-align: left;
            padding: 1rem;
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 0.5rem;
            font-weight: 600;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: var(--transition);
            color: var(--text);
            box-shadow: var(--shadow-sm);
        }

        .collapsible:hover {
            background: var(--primary-light);
        }

        .badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.25rem 0.625rem;
            border-radius: 1rem;
            font-size: 0.75rem;
            font-weight: 600;
            color: white;
            margin-left: 0.5rem;
        }

        .badge.active {
            background: var(--primary);
        }

        .badge.empty {
            background: var(--text-light);
        }

        .toggle-icon {
            font-size: 0.875rem;
            transition: transform 0.3s ease;
        }

        .assigned-list {
            width: 100%;
            margin-top: 1rem;
            position: relative;
        }

        .assigned-item {
            background: var(--card-bg);
            padding: 1rem;
            margin: 0.75rem 0;
            border-radius: 0.5rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-left: 3px solid var(--primary);
            transition: var(--transition);
            box-shadow: var(--shadow-sm);
        }

        .assigned-item:hover {
            background: var(--primary-light);
            transform: translateX(3px);
            box-shadow: var(--shadow);
        }

        .assigned-info {
            display: flex;
            flex-direction: column;
            gap: 0.25rem;
        }

        .assigned-actions {
            display: flex;
            gap: 0.5rem;
        }
.logout-btn {
  margin-left: 1rem;
  background-color: var(--accent-color, #007bff);
  color: white;
  border: none;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  cursor: pointer;
  font-weight: bold;
  transition: background-color 0.3s ease;
}

.logout-btn:hover {
  background-color: var(--accent-hover, #0056b3);
}

        .small-btn {
            padding: 0.5rem 0.75rem;
            font-size: 0.75rem;
            border-radius: 0.375rem;
            box-shadow: var(--shadow-sm);
        }

        .empty-state {
            padding: 2rem;
            text-align: center;
            color: var(--text-light);
            background: var(--card-bg);
            border-radius: 0.5rem;
            margin-top: 0.75rem;
            border: 1px dashed var(--border);
        }

        @media (max-width: 900px) {
            .viva-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 600px) {
            .main-content {
                padding: 1rem;
            }

            .actions {
                grid-template-columns: 1fr;
            }

            .assigned-item {
                flex-direction: column;
                align-items: flex-start;
                gap: 1rem;
            }

            .assigned-actions {
                width: 100%;
                justify-content: flex-end;
            }

            .header {
                padding: 0.75rem 1rem;
            }

            .admin-profile {
                padding: 0.375rem 0.75rem;
            }
        }
    </style>
</head>

<body>
   <header>
  <div class="logo-container">
    <span class="logo-text">JDT PORTAL</span>
  </div>

  <div class="admin-section">
    <div class="admin-profile">
      <div class="profile-icon">👤</div>
      <span class="admin-name">{{adminName}}</span>
          <form action="/Admin/logout" method="POST">
      <button type="submit" class="logout-btn">Log Out</button>
    </form>
    </div>

  </div>
</header>


    <div class="main-content">
        <div class="actions-bar">
            <h1 class="section-title">
                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M3 3v18h18"></path>
                    <path d="M18.4 9.6a9 9 0 1 1-17 4.9"></path>
                    <path d="m2 12 4-4 4 4"></path>
                    <circle cx="18" cy="18" r="3"></circle>
                    <path d="M18 14v4h-4"></path>
                </svg>
                Viva Management
            </h1>
            <a href="/Admin/viva" class="btn">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M12 5v14"></path>
                    <path d="M5 12h14"></path>
                </svg>
                Create New Viva
            </a>

 
        </div>

        <div class="viva-grid">
            {{#each viva}}
            <div class="viva-card">
                <div class="viva-header">
                    {{this.subject_name}}
                    <span class="viva-badge">{{this.type}}</span>
                </div>
                <div class="viva-content">
                    <div class="viva-info">
                        <span class="viva-info-label">Viva Name:</span>
                        <span class="viva-info-value">{{this.viva_name}}</span>
                        
                        <span class="viva-info-label">Viva UUID:</span>
                        <span class="viva-info-value">{{this.viva_uid}}</span>
                    </div>

                    <div class="actions">
                        <a href="/Admin/assign?viva={{this.viva_uid}}" class="action-btn assign">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <rect width="18" height="18" x="3" y="3" rx="2"></rect>
                                <path d="m9 14 2 2 4-4"></path>
                            </svg>
                            Assign Class
                        </a>
                        <a href="/Admin/editViva?viva={{this.viva_uid}}&admin={{../adminName}}" class="action-btn view-assigned">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M17 3a2.85 2.85 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"></path>
                            </svg>
                            Edit Details
                        </a>
                        
                        {{#if (eq this.type "MCQ")}}
                        <a href="/Admin/result?viva_uid={{this.viva_uid}}&network_name={{../adminName}}" class="action-btn results">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M3 3v18h18"></path>
                                <path d="m19 9-5 5-4-4-3 3"></path>
                            </svg>
                            Check Results
                        </a>
                        {{else}}
                                   <a class="action-btn results" href="/Admin/score-viva?vivaUid={{this.viva_uid}}&networkName={{../adminName}}">    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M3 3v18h18"></path>
                                <path d="m19 9-5 5-4-4-3 3"></path>
                                
                            </svg>
                            Validate Results</a>
                        <a href="/Admin/ddresult?vivaUid={{this.viva_uid}}&networkName={{../adminName}}" class="action-btn results">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M3 3v18h18"></path>
                                <path d="m19 9-5 5-4-4-3 3"></path>
                                
                            </svg>
                            Check Results
                        </a>
                        {{/if}}
                        
                        <a href="/Admin/delete-viva?admin={{../adminName}}&viva={{this.viva_uid}}" class="action-btn delete">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                <path d="M3 6h18"></path>
                                <path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"></path>
                                <path d="M8 6V4c0-1 1-2 2-2h4c1 0 2 1 2 2v2"></path>
                                <line x1="10" x2="10" y1="11" y2="17"></line>
                                <line x1="14" x2="14" y1="11" y2="17"></line>
                            </svg>
                            Delete Viva
                        </a>
                    </div>

                    <div class="collapsible-section">
                        <button class="collapsible">
                            <div>
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-right: 8px;">
                                    <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"></path>
                                    <circle cx="9" cy="7" r="4"></circle>
                                    <path d="M22 21v-2a4 4 0 0 0-3-3.87"></path>
                                    <path d="M16 3.13a4 4 0 0 1 0 7.75"></path>
                                </svg>
                                Assigned Classes
                                {{#if this.logs.length}}
                                <span class="badge active">{{this.logs.length}}</span>
                                {{else}}
                                <span class="badge empty">0</span>
                                {{/if}}
                            </div>
                            <span class="toggle-icon">▼</span>
                        </button>
                        <div class="assigned-list" style="display: none;">
                            {{#if this.logs.length}}
                            {{#each this.logs}}
                            <div class="assigned-item">
                                <div class="assigned-info">
                                    <div>
                                        <strong>Class:</strong> {{this.className}}
                                        <span class="badge active">{{this.rollRange}}</span>
                                    </div>
                                    <div style="font-size: 0.875rem; color: var(--text-light);">
                                        <strong>Login ID:</strong> {{this.uniqueId}}
                                    </div>
                                </div>
                                <div class="assigned-actions">
                                    <a href="/Admin/studentpc?uniqueId={{../this.viva_uid}}&logId={{this.uniqueId}}" class="action-btn small-btn view-assigned">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                            <path d="M2 12s3-7 10-7 10 7 10 7-3 7-10 7-10-7-10-7Z"></path>
                                            <circle cx="12" cy="12" r="3"></circle>
                                        </svg>
                                        Details
                                    </a>
                                    <a href="/Admin/remove-class?admin={{../../adminName}}&class={{this.className}}&range={{this.rollRange}}&uniqueId={{this.uniqueId}}" class="action-btn small-btn remove">
                                        <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                                            <path d="M3 6h18"></path>
                                            <path d="M19 6v14c0 1-1 2-2 2H7c-1 0-2-1-2-2V6"></path>
                                        </svg>
                                        Remove
                                    </a>
                                </div>
                            </div>
                            {{/each}}
                            {{else}}
                            <div class="empty-state">
                                <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="margin-bottom: 8px;">
                                    <rect width="18" height="18" x="3" y="3" rx="2" ry="2"></rect>
                                    <line x1="9" y1="9" x2="15" y2="15"></line>
                                    <line x1="15" y1="9" x2="9" y2="15"></line>
                                </svg>
                                <p>No assigned classes</p>
                            </div>
                            {{/if}}
                        </div>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    <script>
        document.querySelectorAll(".delete").forEach(button => {
            button.addEventListener("click", function(event) {
                if (!confirm("Are you sure you want to delete this viva? This action cannot be undone.")) {
                    event.preventDefault();
                }
            });
        });

        // Collapsible functionality for assigned classes
        document.querySelectorAll('.collapsible').forEach(function(collapsible) {
            collapsible.addEventListener('click', function() {
                const content = this.nextElementSibling;
                const toggleIcon = this.querySelector('.toggle-icon');

                if (content.style.display === 'none') {
                    content.style.display = 'block';
                    toggleIcon.textContent = '▼';
                    toggleIcon.style.transform = 'rotate(0deg)';
                } else {
                    content.style.display = 'none';
                    toggleIcon.textContent = '►';
                    toggleIcon.style.transform = 'rotate(-90deg)';
                }
            });
        });
        // Add this after $(document).ready(function() {
    // Get URL parameters and set them in all forms
    const urlParams = new URLSearchParams(window.location.search);
    const vivaUid = urlParams.get('vivaUid');
    const networkName = urlParams.get('networkName');
    
    if (vivaUid) {
        $('input[name="vivaUid"]').val(vivaUid);
    }
    
    if (networkName) {
        $('input[name="networkName"]').val(networkName);
    }
    
    // Ensure className is set properly in each form
    $('.class-section').each(function() {
        const className = $(this).find('.class-header').data('classname');
        $(this).find('input[name="className"]').val(className);
    });
    
    // Log what values we're setting
    console.log('Setting form values from URL:', {
        vivaUid: vivaUid,
        networkName: networkName
    });
    </script>
</body>

</html>