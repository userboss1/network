<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viva Results - Dashboard</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        /* --- All previous styles are the same, with the additions below --- */
        
        /* Basic Reset & Font */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background: #f4f7f6;
            color: #333;
            min-height: 100vh;
            padding: 25px;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            width: 100%;
            background: #ffffff;
            border-radius: 15px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
            overflow: hidden;
            transform: translateY(0);
            animation: fadeInSlideIn 0.6s ease-out forwards;
        }

        .header {
            background: linear-gradient(135deg, #4a90e2 0%, #5d6edc 100%);
            color: white;
            padding: 40px 30px;
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .header h1 {
            font-size: 2.8em;
            margin-bottom: 15px;
            font-weight: 700;
            text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.15);
        }

        .viva-info {
            background: rgba(255, 255, 255, 0.18);
            padding: 25px;
            border-radius: 12px;
            margin-top: 25px;
            backdrop-filter: blur(8px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }

        .viva-info:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }

        .viva-info h2 {
            font-size: 1.6em;
            margin-bottom: 18px;
            font-weight: 600;
            color: #fff;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 12px;
            margin-top: 15px;
        }

        .info-item {
            background: rgba(255, 255, 255, 0.1);
            padding: 15px;
            border-radius: 10px;
            text-align: center;
            border: 1px solid rgba(255, 255, 255, 0.15);
            transition: background 0.2s ease;
        }

        .info-item:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .info-label {
            font-size: 0.9em;
            opacity: 0.9;
            margin-bottom: 6px;
            color: #e0e0e0;
        }

        .info-value {
            font-size: 1.2em;
            font-weight: 600;
            color: #fff;
        }

        .stats-section {
            padding: 30px;
            background: #ffffff;
            border-bottom: 1px solid #e9ecef;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 20px;
        }

        .stat-card {
            background: #fdfdfd;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
            text-align: center;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
            border: 1px solid #e9ecef;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .stat-number {
            font-size: 2.8em;
            font-weight: 700;
            color: #007bff;
            margin-bottom: 10px;
        }

        .stat-label {
            font-size: 1.1em;
            color: #6c757d;
            font-weight: 500;
        }

        .results-section {
            padding: 30px;
            background: #ffffff;
            border-radius: 0 0 15px 15px;
        }

        .section-title {
            font-size: 2em;
            color: #333;
            margin-bottom: 30px;
            text-align: center;
            font-weight: 700;
            position: relative;
            padding-bottom: 10px;
        }

        .section-title::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: 0;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: #007bff;
            border-radius: 2px;
        }

        .results-grid {
            display: grid;
            grid-template-columns: 1fr; /* Changed to single column for better accordion flow */
            gap: 20px; /* Adjusted gap */
        }

        .result-card {
            background: #fdfdfd;
            border-radius: 12px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.06);
            overflow: hidden;
            transition: box-shadow 0.2s ease; /* Simplified transition */
            border: 1px solid #e9ecef;
        }

        /* MODIFICATION: Add hover effect to the header now that it's clickable */
        .result-header {
            background: #28a745;
            color: white;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(0,0,0,0.1);
            cursor: pointer; /* Make it look clickable */
            transition: background-color 0.2s ease;
        }
        .result-header:hover {
            background: #218838; /* Darker green on hover */
        }

        .student-info-container h3 { /* Changed from .student-info h3 to avoid conflicts */
            font-size: 1.4em;
            margin-bottom: 6px;
            font-weight: 600;
        }

        .student-details {
            font-size: 0.85em;
            opacity: 0.9;
            line-height: 1.4;
        }

        .score-badge {
            background: rgba(255,255,255,0.25);
            padding: 15px 20px;
            border-radius: 10px;
            text-align: center;
            backdrop-filter: blur(5px);
            border: 1px solid rgba(255,255,255,0.3);
            flex-shrink: 0; /* Prevent score badge from shrinking */
            margin-left: 15px; /* Add space between info and score */
        }
        .score-number {
            font-size: 2.2em;
            font-weight: 700;
            margin-bottom: 5px;
        }
        .score-label {
            font-size: 0.9em;
            opacity: 0.9;
        }

        /* NEW: Styles for the toggle button icon */
        .toggle-icon {
            font-size: 1.4em;
            margin-left: 20px;
            transition: transform 0.4s cubic-bezier(0.25, 0.1, 0.25, 1); /* Smooth rotation */
        }
        
        /* NEW: Rotate icon when the card is open */
        .result-card.is-open .toggle-icon {
            transform: rotate(180deg);
        }

        /* MODIFICATION: Make the result body collapsible */
        .result-body {
            /* Hide by default using max-height for smooth transition */
            max-height: 0;
            overflow: hidden;
            padding: 0 25px; /* Remove vertical padding when closed */
            transition: max-height 0.5s ease-in-out, padding 0.5s ease-in-out;
            background-color: #ffffff; /* Explicit background */
        }
        
        /* NEW: Styles for when the card is open */
        .result-card.is-open .result-body {
            max-height: 1500px; /* A large value to allow for expansion */
            padding: 25px; /* Restore padding when open */
            border-top: 1px solid #e9ecef;
        }

        .answer-list {
            display: grid;
            gap: 15px;
        }
        .answer-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            border-left: 4px solid #007bff;
            box-shadow: 0 1px 5px rgba(0,0,0,0.03);
        }
        .question-text {
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
            font-size: 1em;
        }
        .answer-text {
            color: #555;
            margin-bottom: 8px;
            font-size: 0.95em;
        }
        .answer-score {
            display: inline-block;
            background: #6c757d;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 500;
        }
        
        /* --- Other styles remain the same --- */
        .no-results, .no-results i, .no-results h2, .no-results p, @keyframes fadeInSlideIn { /* Unchanged styles condensed */
            text-align: center; padding: 80px 30px; color: #6c757d; background: #ffffff; border-radius: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.05); margin-top: 20px;
        }
        .no-results i { font-size: 5em; margin-bottom: 25px; opacity: 0.4; color: #adb5bd; }
        .no-results h2 { font-size: 2em; color: #333; margin-bottom: 10px; }
        .no-results p { font-size: 1.1em; color: #6c757d; }
        @keyframes fadeInSlideIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        /* Responsive Adjustments */
        @media (max-width: 768px) {
            body { padding: 15px; }
            .container { border-radius: 10px; }
            .header { padding: 30px 20px; }
            .header h1 { font-size: 2.2em; }
            .viva-info { padding: 18px; margin-top: 18px; }
            .viva-info h2 { font-size: 1.3em; }
            .info-grid, .stats-grid, .results-grid { grid-template-columns: 1fr; }
            .stats-section, .results-section { padding: 20px; }
            .section-title { font-size: 1.6em; }
            /* MODIFICATION: Adjust result header for mobile */
            .result-header {
                padding: 15px;
                align-items: center; /* Vertically center items */
            }
            .student-info-container h3 { font-size: 1.2em; }
            .score-badge { padding: 10px 12px; }
            .score-number { font-size: 1.6em; }
            .result-card.is-open .result-body { padding: 20px; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📊 Viva Results Dashboard</h1>
            {{#if results.length}}
            <div class="viva-info">
                <h2>{{results.0.vivaInfo.viva_name}}</h2>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">Subject</div>
                        <div class="info-value">{{results.0.vivaInfo.subject_name}}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Teacher</div>
                        <div class="info-value">{{results.0.vivaInfo.network_name}}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Type</div>
                        <div class="info-value">{{results.0.vivaInfo.type}}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Viva ID</div>
                        <div class="info-value">{{vivaUid}}</div>
                    </div>
                </div>
            </div>
            {{/if}}
        </div>

        {{#if results.length}}
        <div class="stats-section">
            <div class="stats-grid">
                <div class="stat-card"><div class="stat-number">{{results.length}}</div><div class="stat-label">Total Students</div></div>
                <div class="stat-card"><div class="stat-number">{{results.0.totalQuestions}}</div><div class="stat-label">Total Questions</div></div>
                <div class="stat-card"><div class="stat-number">{{results.0.totalMarks}}</div><div class="stat-label">Total Marks</div></div>
                <div class="stat-card"><div class="stat-number">{{#each results}}{{#if @first}}{{percentage}}%{{/if}}{{/each}}</div><div class="stat-label">First Student %</div></div>
            </div>
        </div>

        <div class="results-section">
            <h2 class="section-title">📋 Individual Results</h2>
            <div class="results-grid">
                {{#each results}}
                <div class="result-card">
                    <div class="result-header">
                        <div class="student-info-container" style="flex-grow: 1;">
                            <h3>{{studentInfo.name}}</h3>
                            <div class="student-details">
                                <div>Roll: {{studentInfo.roll}} | Register: {{studentInfo.register}}</div>
                                <div>Class: {{studentInfo.className}}</div>
                            </div>
                        </div>
                        <div class="score-badge">
                            <div class="score-number">{{score}}/{{totalMarks}}</div>
                            <div class="score-label">{{percentage}}%</div>
                        </div>
                        <i class="fas fa-chevron-down toggle-icon"></i>
                    </div>
                    <div class="result-body">
                        <div class="answer-list">
                            {{#each answers}}
                            <div class="answer-item">
                                <div class="question-text">Q{{add @index 1}}: {{question}}</div>
                                <div class="answer-text"><strong>Answer:</strong> {{selectedOption}}</div>
                                <div class="answer-score">{{score}}/{{totalScore}} marks</div>
                            </div>
                            {{/each}}
                        </div>
                        <div style="margin-top: 20px; text-align: center; font-size: 0.9em; color: #64748b;">
                            Submitted: {{submittedAt}}
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{else}}
        <div class="no-results">
            <div style="font-size: 4em; margin-bottom: 20px; opacity: 0.5;">📋</div>
            <h2>No Results Found</h2>
            <p>No students have submitted results for Viva ID: {{vivaUid}}</p>
        </div>
        {{/if}}
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            // Find all the result card headers
            const resultHeaders = document.querySelectorAll('.result-header');

            resultHeaders.forEach(header => {
                header.addEventListener('click', () => {
                    // Find the parent .result-card of the clicked header
                    const resultCard = header.closest('.result-card');
                    
                    // Toggle the .is-open class on the card
                    resultCard.classList.toggle('is-open');
                });
            });
        });

        // A simple Handlebars helper to add 1 to the index for question numbers
        // This should be registered in your backend/Node.js server where you render the template
        /*
        Handlebars.registerHelper('add', function(a, b) {
            return a + b;
        });
        */
    </script>
</body>
</html>