<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Viva Results - Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Google+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Google Sans', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #f8f9fa;
            color: #202124;
            min-height: 100vh;
            line-height: 1.6;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 24px;
        }

        /* Header Section */
        .header {
            background: linear-gradient(135deg, #4285f4 0%, #34a853 100%);
            color: white;
            padding: 48px 32px;
            border-radius: 16px;
            margin-bottom: 32px;
            box-shadow: 0 8px 24px rgba(66, 133, 244, 0.15);
            position: relative;
            overflow: hidden;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 200px;
            height: 200px;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            border-radius: 50%;
            transform: translate(50%, -50%);
        }

        .header h1 {
            font-size: 2.5rem;
            font-weight: 500;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .header-subtitle {
            font-size: 1.1rem;
            opacity: 0.9;
            font-weight: 400;
        }

        .viva-info {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 12px;
            padding: 24px;
            margin-top: 32px;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .viva-info:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 32px rgba(0, 0, 0, 0.15);
        }

        .viva-info h2 {
            font-size: 1.5rem;
            font-weight: 500;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .info-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
        }

        .info-item {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 8px;
            padding: 16px;
            text-align: center;
            transition: all 0.2s ease;
        }

        .info-item:hover {
            background: rgba(255, 255, 255, 0.15);
        }

        .info-label {
            font-size: 0.875rem;
            opacity: 0.8;
            margin-bottom: 4px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .info-value {
            font-size: 1.1rem;
            font-weight: 500;
        }

        /* Stats Section */
        .stats-section {
            margin-bottom: 32px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 20px;
        }

        .stat-card {
            background: white;
            border-radius: 12px;
            padding: 24px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #e8eaed;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #4285f4, #34a853, #fbbc05, #ea4335);
        }

        .stat-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 600;
            color: #4285f4;
            margin-bottom: 8px;
        }

        .stat-label {
            font-size: 1rem;
            color: #5f6368;
            font-weight: 500;
        }

        /* Results Section */
        .results-section {
            margin-bottom: 32px;
        }

        .section-title {
            font-size: 1.75rem;
            font-weight: 500;
            color: #202124;
            margin-bottom: 24px;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .results-grid {
            display: grid;
            gap: 16px;
        }

        .result-card {
            background: white;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #e8eaed;
            overflow: hidden;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .result-card:hover {
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
        }

        .result-header {
            padding: 20px 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: pointer;
            transition: all 0.2s ease;
            background: white;
            border-bottom: 1px solid #e8eaed;
        }

        .result-header:hover {
            background: #f8f9fa;
        }

        .student-info-container {
            flex-grow: 1;
        }

        .student-info-container h3 {
            font-size: 1.25rem;
            font-weight: 500;
            color: #202124;
            margin-bottom: 4px;
        }

        .student-details {
            font-size: 0.875rem;
            color: #5f6368;
            line-height: 1.4;
        }

        .score-badge {
            background: linear-gradient(135deg, #4285f4 0%, #34a853 100%);
            color: white;
            padding: 12px 20px;
            border-radius: 24px;
            text-align: center;
            margin-left: 20px;
            min-width: 100px;
            box-shadow: 0 2px 8px rgba(66, 133, 244, 0.2);
        }

        .score-number {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 2px;
        }

        .score-label {
            font-size: 0.875rem;
            opacity: 0.9;
        }

        .toggle-icon {
            margin-left: 16px;
            transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            color: #5f6368;
        }

        .result-card.is-open .toggle-icon {
            transform: rotate(180deg);
        }

        .result-body {
            max-height: 0;
            overflow: hidden;
            padding: 0 24px;
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            background: #fafbfc;
        }

        .result-card.is-open .result-body {
            max-height: 2000px;
            padding: 24px;
        }

        .answer-list {
            display: grid;
            gap: 16px;
        }

        .answer-item {
            background: white;
            border-radius: 8px;
            padding: 20px;
            border: 1px solid #e8eaed;
            transition: all 0.2s ease;
        }

        .answer-item:hover {
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }

        .question-text {
            font-weight: 500;
            color: #202124;
            margin-bottom: 12px;
            font-size: 1rem;
            display: flex;
            align-items: flex-start;
            gap: 8px;
        }

        .question-number {
            background: #4285f4;
            color: white;
            font-size: 0.75rem;
            padding: 4px 8px;
            border-radius: 12px;
            font-weight: 600;
            min-width: 24px;
            text-align: center;
            flex-shrink: 0;
        }

        .answer-text {
            color: #5f6368;
            margin-bottom: 12px;
            font-size: 0.95rem;
            line-height: 1.5;
        }

        .answer-score {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            background: #e8f0fe;
            color: #1967d2;
            padding: 6px 12px;
            border-radius: 16px;
            font-size: 0.875rem;
            font-weight: 500;
        }

        .submission-info {
            margin-top: 24px;
            padding-top: 20px;
            border-top: 1px solid #e8eaed;
            text-align: center;
            color: #5f6368;
            font-size: 0.875rem;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .no-results {
            text-align: center;
            padding: 64px 32px;
            background: white;
            border-radius: 16px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            border: 1px solid #e8eaed;
        }

        .no-results .material-icons {
            font-size: 64px;
            color: #9aa0a6;
            margin-bottom: 16px;
        }

        .no-results h2 {
            font-size: 1.5rem;
            font-weight: 500;
            color: #202124;
            margin-bottom: 8px;
        }

        .no-results p {
            color: #5f6368;
            font-size: 1rem;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .result-card {
            animation: fadeInUp 0.5s ease-out forwards;
        }

        .result-card:nth-child(1) { animation-delay: 0.1s; }
        .result-card:nth-child(2) { animation-delay: 0.2s; }
        .result-card:nth-child(3) { animation-delay: 0.3s; }
        .result-card:nth-child(4) { animation-delay: 0.4s; }
        .result-card:nth-child(5) { animation-delay: 0.5s; }

        /* Responsive Design */
        @media (max-width: 768px) {
            .container {
                padding: 16px;
            }

            .header {
                padding: 32px 20px;
            }

            .header h1 {
                font-size: 2rem;
            }

            .info-grid {
                grid-template-columns: 1fr;
            }

            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .result-header {
                padding: 16px 20px;
                flex-direction: column;
                align-items: flex-start;
                gap: 12px;
            }

            .score-badge {
                margin-left: 0;
                align-self: flex-start;
            }

            .toggle-icon {
                position: absolute;
                top: 16px;
                right: 20px;
                margin-left: 0;
            }
        }

        @media (max-width: 480px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>
                <span class="material-icons" style="font-size: 2.5rem;">assessment</span>
                Viva Results Dashboard
            </h1>
            <p class="header-subtitle">Comprehensive examination results and analytics</p>
            
            {{#if results.length}}
            <div class="viva-info">
                <h2>
                    <span class="material-icons">school</span>
                    {{results.0.vivaInfo.viva_name}}
                </h2>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-label">Subject</div>
                        <div class="info-value">{{results.0.vivaInfo.subject_name}}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Teacher</div>
                        <div class="info-value">{{results.0.vivaInfo.network_name}}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Type</div>
                        <div class="info-value">{{results.0.vivaInfo.type}}</div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Viva ID</div>
                        <div class="info-value">{{vivaUid}}</div>
                    </div>
                </div>
            </div>
            {{/if}}
        </div>

        {{#if results.length}}
        <div class="stats-section">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-number">{{results.length}}</div>
                    <div class="stat-label">Total Students</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">{{results.0.totalQuestions}}</div>
                    <div class="stat-label">Total Questions</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">{{results.0.totalMarks}}</div>
                    <div class="stat-label">Total Marks</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number">{{#each results}}{{#if @first}}{{percentage}}%{{/if}}{{/each}}</div>
                    <div class="stat-label">Top Score</div>
                </div>
            </div>
        </div>

        <div class="results-section">
            <h2 class="section-title">
                <span class="material-icons">assignment</span>
                Individual Results
            </h2>
            <div class="results-grid">
                {{#each results}}
                <div class="result-card">
                    <div class="result-header">
                        <div class="student-info-container">
                            <h3>{{studentInfo.name}}</h3>
                            <div class="student-details">
                                <div>Roll: {{studentInfo.roll}} | Register: {{studentInfo.register}}</div>
                                <div>Class: {{studentInfo.className}}</div>
                            </div>
                        </div>
                        <div class="score-badge">
                            <div class="score-number">{{score}}/{{totalMarks}}</div>
                            <div class="score-label">{{percentage}}%</div>
                        </div>
                        <span class="material-icons toggle-icon">expand_more</span>
                    </div>
                    <div class="result-body">
                        <div class="answer-list">
                            {{#each answers}}
                            <div class="answer-item">
                                <div class="question-text">
                                    <span class="question-number">{{add @index 1}}</span>
                                    <span>{{question}}</span>
                                </div>
                                <div class="answer-text"><strong>Answer:</strong> {{selectedOption}}</div>
                                <div class="answer-score">
                                    <span class="material-icons" style="font-size: 16px;">{{#if score}}check_circle{{else}}error{{/if}}</span>
                                    {{score}}/{{totalScore}} marks
                                </div>
                            </div>
                            {{/each}}
                        </div>
                        <div class="submission-info">
                            <span class="material-icons">schedule</span>
                            <span>Submitted: {{submittedAt}}</span>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{else}}
        <div class="no-results">
            <span class="material-icons">assignment</span>
            <h2>No Results Found</h2>
            <p>No students have submitted results for Viva ID: {{vivaUid}}</p>
        </div>
        {{/if}}
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const resultHeaders = document.querySelectorAll('.result-header');

            resultHeaders.forEach(header => {
                header.addEventListener('click', () => {
                    const resultCard = header.closest('.result-card');
                    const isCurrentlyOpen = resultCard.classList.contains('is-open');
                    
                    // Close all other cards
                    document.querySelectorAll('.result-card.is-open').forEach(card => {
                        if (card !== resultCard) {
                            card.classList.remove('is-open');
                        }
                    });
                    
                    // Toggle current card
                    resultCard.classList.toggle('is-open');
                    
                    // Add subtle animation feedback
                    if (!isCurrentlyOpen) {
                        resultCard.scrollIntoView({ 
                            behavior: 'smooth', 
                            block: 'nearest' 
                        });
                    }
                });
            });

            // Add keyboard navigation
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape') {
                    document.querySelectorAll('.result-card.is-open').forEach(card => {
                        card.classList.remove('is-open');
                    });
                }
            });
        });

        // Handlebars helper for adding numbers (should be registered server-side)
        // Handlebars.registerHelper('add', function(a, b) {
        //     return a + b;
        // });
    </script>
</body>
</html>