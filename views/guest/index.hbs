<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Viva Details</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --google-blue: #1a73e8;
            --google-blue-hover: #185abc;
            --background-color: #f8f9fa;
            --surface-color: #ffffff;
            --text-primary: #202124;
            --text-secondary: #5f6368;
            --border-color: #dadce0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: var(--background-color);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            /* Decorative background pattern */
            background-image:
                radial-gradient(circle at 15% 85%, rgba(26, 115, 232, 0.05), transparent 20%),
                radial-gradient(circle at 85% 15%, rgba(26, 115, 232, 0.05), transparent 20%);
        }

        .container {
            background-color: var(--surface-color);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            box-shadow: 0 1px 2px rgba(60,64,67,.3), 0 2px 6px 2px rgba(60,64,67,.15);
            width: 100%;
            max-width: 650px;
            text-align: center;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInSlideUp 0.7s ease-out forwards;
        }

        @keyframes fadeInSlideUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .header {
            padding: 2rem 2.5rem;
            border-bottom: 1px solid var(--border-color);
        }

        .header h1 {
            color: var(--text-primary);
            font-size: 1.75rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .header p {
            color: var(--text-secondary);
            font-size: 1rem;
        }
        
        .viva-name {
            background-color: #e8f0fe;
            color: var(--google-blue);
            padding: 0.75rem 1.5rem;
            margin: 1.5rem 2.5rem;
            border-radius: 8px;
            font-weight: 500;
            font-size: 1.1rem;
            border: 1px solid #d2e3fc;
        }

        .details-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            padding: 0 2.5rem;
            margin-bottom: 2rem;
            text-align: left;
        }

        .detail-item {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .detail-icon {
            flex-shrink: 0;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #e8f0fe;
            border-radius: 50%;
        }
        .detail-icon svg {
            width: 24px;
            height: 24px;
            fill: var(--google-blue);
        }

        .detail-text .label {
            font-size: 0.875rem;
            color: var(--text-secondary);
        }
        .detail-text .value {
            font-size: 1rem;
            font-weight: 500;
            color: var(--text-primary);
        }

        .action-container {
            padding: 1.5rem 2.5rem 2.5rem 2.5rem;
            background-color: #f8f9fa;
            border-top: 1px solid var(--border-color);
        }

        .btn {
            width: 100%;
            background-color: var(--google-blue);
            color: white;
            border: none;
            padding: 14px 24px;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            animation: pulse-button 2s infinite ease-in-out;
        }

        .btn:hover {
            box-shadow: 0 1px 2px 0 rgba(60,64,67,.3), 0 1px 3px 1px rgba(60,64,67,.15);
            background-color: var(--google-blue-hover);
            animation-play-state: paused; /* Pause pulse on hover */
        }
        
        @keyframes pulse-button {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }

        .footer {
            margin-top: 1rem;
            color: var(--text-secondary);
            font-size: 0.875rem;
        }

    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Test Session Details</h1>
            <p>Please verify your information before proceeding.</p>
        </div>

        <div class="viva-name">
            {{user.vivaname}}
        </div>

        <div class="details-grid">
            <!-- Student Info -->
            <div class="detail-item">
                <div class="detail-icon"><svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/></svg></div>
                <div class="detail-text">
                    <div class="label">Student Name</div>
                    <div class="value">{{user.name}}</div>
                </div>
            </div>
            <div class="detail-item">
                <div class="detail-icon"><svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M20 4H4c-1.11 0-1.99.89-1.99 2L2 18c0 1.11.89 2 2 2h16c1.11 0 2-.89 2-2V6c0-1.11-.89-2-2-2zm0 14H4v-6h16v6zm0-10H4V6h16v2z"/></svg></div>
                <div class="detail-text">
                    <div class="label">Roll Number</div>
                    <div class="value">{{user.roll}}</div>
                </div>
            </div>
            <div class="detail-item">
                <div class="detail-icon"><svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M12 3L1 9l4 2.18v6L12 21l7-3.82v-6l2-1.09V17h2V9L12 3zm6.82 6L12 12.72 5.18 9 12 5.28 18.82 9zM17 15.99l-5 2.73-5-2.73v-3.72L12 15l5-2.73v3.72z"/></svg></div>
                <div class="detail-text">
                    <div class="label">Class</div>
                    <div class="value">{{user.className}}</div>
                </div>
            </div>
            <!-- Viva Info -->
            <div class="detail-item">
                <div class="detail-icon"><svg xmlns="http://www.w3.org/2000/svg" enable-background="new 0 0 24 24" height="24px" viewBox="0 0 24 24" width="24px"><g><rect fill="none" height="24" width="24"/></g><g><g><path d="M14,2H6C4.9,2,4,2.9,4,4v16c0,1.1,0.9,2,2,2h12c1.1,0,2-0.9,2-2V8L14,2z M16,16H8v-2h8V16z M16,12H8v-2h8V12z M13,9V3.5 L18.5,9H13z"/></g></g></svg></div>
                <div class="detail-text">
                    <div class="label">Instructor</div>
                    <div class="value">{{user.networkName}}</div>
                </div>
            </div>
            <div class="detail-item">
                <div class="detail-icon"><svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M22 6h-6V4c0-1.1-.9-2-2-2h-4c-1.1 0-2 .9-2 2v2H2v15h20V6zm-8-2h4v2h-4V4zM4 8h16v11H4V8zm12 2h-4v3h4v-3z"/></svg></div>
                <div class="detail-text">
                    <div class="label">Register Number</div>
                    <div class="value">{{user.register}}</div>
                </div>
            </div>
            <div class="detail-item">
                <div class="detail-icon"><svg xmlns="http://www.w3.org/2000/svg" height="24px" viewBox="0 0 24 24" width="24px"><path d="M0 0h24v24H0V0z" fill="none"/><path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67z"/></svg></div>
                <div class="detail-text">
                    <div class="label">Duration</div>
                    <div class="value">{{user.duration}} minutes</div>
                </div>
            </div>
        </div>

        <div class="action-container">
            <form action="/attendviva" method="get">
                <button type="submit" class="btn">Start  Examination</button>
            </form>
            <div class="footer">
                Ensure you are prepared before starting the exam.
            </div>
        </div>
    </div>
</body>
</html>
