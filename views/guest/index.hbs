<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Viva Details</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            color: #e0e0e0;
        }

        .container {
            background: rgba(42, 49, 73, 0.95);
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.4);
            text-align: center;
            width: 700px;
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
        }

        .header {
            margin-bottom: 30px;
            position: relative;
            padding-bottom: 15px;
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
        }

        .header h1 {
            color: #ffffff;
            margin: 0;
            font-size: 32px;
            font-weight: 600;
        }

        .header p {
            color: #b8c6db;
            margin: 8px 0 0;
            font-size: 16px;
        }

        .viva-name {
            background: linear-gradient(135deg, #283c86, #45a247);
            color: white;
            padding: 12px 20px;
            border-radius: 10px;
            margin-bottom: 25px;
            font-weight: 600;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
            font-size: 18px;
        }

        .details-container {
            display: flex;
            margin-bottom: 30px;
            gap: 25px;
        }

        .details {
            background-color: rgba(255, 255, 255, 0.08);
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.15);
            flex: 1;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 16px;
            padding-bottom: 16px;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .detail-item:last-child {
            margin-bottom: 0;
            padding-bottom: 0;
            border-bottom: none;
        }

        .detail-label {
            font-weight: 600;
            color: #b8c6db;
            text-align: left;
            font-size: 16px;
        }

        .detail-value {
            color: #ffffff;
            font-weight: 500;
            text-align: right;
            font-size: 16px;
        }

        .action-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        button {
            padding: 14px 26px;
            border: none;
            border-radius: 10px;
            font-size: 18px;
            font-weight: 600;
            cursor: pointer;
            background: linear-gradient(135deg, #283c86, #45a247);
            color: white;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease;
        }

        button:hover {
            background: linear-gradient(135deg, #314b9e, #4ab94e);
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
        }

        .timer {
            font-size: 16px;
            color: #b8c6db;
            margin-top: 20px;
        }

        .badge {
            display: inline-block;
            background-color: rgba(255, 255, 255, 0.15);
            color: #ffffff;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            margin-left: 5px;
        }

        .footer {
            margin-top: 20px;
            color: #b8c6db;
            font-size: 14px;
        }

        .student-info {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
        }

        .student-info-col {
            flex: 1;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Viva Session Details</h1>
            <p>Please verify your information before proceeding</p>
        </div>

        <div class="viva-name">
            {{user.vivaname}}
        </div>

        <div class="student-info">
            <div class="details student-info-col">
                <div class="detail-item">
                    <div class="detail-label">Student Name</div>
                    <div class="detail-value">{{user.name}}</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Roll Number</div>
                    <div class="detail-value">{{user.roll}}</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Register Number</div>
                    <div class="detail-value">{{user.register}}</div>
                </div>
            </div>
            
            <div class="details student-info-col">
                <div class="detail-item">
                    <div class="detail-label">Class</div>
                    <div class="detail-value">{{user.className}}</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Instructor</div>
                    <div class="detail-value">{{user.networkName}}</div>
                </div>
                <div class="detail-item">
                    <div class="detail-label">Duration</div>
                    <div class="detail-value">{{user.duration}} <span class="badge">minutes</span></div>
                </div>
            </div>
        </div>

        <div class="action-container">
            <form action="/attendviva" method="get">
                <button type="submit">Start Viva Examination</button>
            </form>

            <div class="timer">
                Your exam will begin once you click the button above
            </div>
        </div>

        <div class="footer">
            Make sure you have all materials ready before starting the viva
        </div>
    </div>
</body>
</html>