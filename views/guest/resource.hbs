<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <style>
        body {
            background-color: #eef2f6;
            font-family: 'Poppins', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            flex-wrap: wrap;
            gap: 20px;
            padding: 20px;
        }

        .card {
            background: #fff;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            max-width: 350px;
            width: 100%;
            text-align: center;
            padding: 30px;
            transition: transform 0.3s ease-in-out;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card-header {
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: white;
            padding: 20px;
            font-size: 1.4rem;
            border-radius: 10px 10px 0 0;
        }

        .card-body {
            padding: 20px;
        }

        .pdf-link {
            display: inline-block;
            padding: 12px 20px;
            background-color: #28a745;
            color: #fff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 1rem;
            transition: background 0.3s;
        }

        .pdf-link:hover {
            background-color: #218838;
        }
    </style>
</head>

<body>
    {{#each user}}
    <div class="card">
        <div class="card-header">
            <h2>{{this.username}}</h2>
        </div>
        <div class="card-body">
            <p class="lead">{{this.content}}</p>
            <a href="/pdf/{{this._id}}.pdf" target="_blank" class="pdf-link">Download/View PDF</a>
        </div>
    </div>
    {{/each}}
</body>

</html>
